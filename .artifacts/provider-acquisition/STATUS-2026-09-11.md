# 資料來源取得率修正 — 2026-09-11

## 已完成

- `/api/observability/provider-sla` 新增 `acquisition` 唯讀 projection。
- 非空成功才進入取得資料率分子；空結果、失敗及降級資料列入抓取分母，快取與彙總列另外計算。
- 保留原始 provider SLA 事件及 legacy availability 欄位，面板不使用 legacy 比率。
- 分開顯示有效快取、空快取及過期快取；不將空結果宣稱為正常回應。
- 全部来源可見，明細可展開全部供應商並優先列失敗；不以備援成功覆蓋期間失敗。
- 顯示統計期間及台灣時間更新時間；全部僅代表已保留事件。

## 驗證

`tests/run_prompt_boundary_tests.py tests/test_provider_acquisition.py tests/test_provider_acquisition_frontend.py tests/test_runtime_observability.py tests/test_static_history_filters.py -q`：隔離 worktree（基底 `1d6463d6`）中 306 passed。

截圖同批事件重播：法說會 0/75=0%，另類數據 11/75=14.7%，全球市場脈絡 75/75=100%；新聞 201/498=40.4%。這些是供應商觀測，不是獨立 HTTP 請求或內容正確率。

Playwright 使用實際面板 JS/CSS、攔截所有網路，確認 18 類來源完整呈現、失敗來源展開可見、1280px/375px 無橫向溢出、無頁面 JavaScript 錯誤。

- `/tmp/provider-acquisition-isolated-tests-20260911.log`（最終隔離驗證，306 passed）
- `/tmp/provider-acquisition-tests-20260911.log`（先前共用 checkout 驗證）
- `/tmp/provider-acquisition-screenshot-replay-20260911.json`
- `/tmp/provider-acquisition-desktop-20260911.png`
- `/tmp/provider-acquisition-mobile-20260911.png`

## 正式服務狀態

本次最後查證正式 API 尚未回傳 `acquisition`，未重載服務。依整合任務明確要求，本次僅提交並推送，不重啟服務；其他任務的報告發布修改已完整排除並保留。若瀏覽器載入新靜態檔但 API 尚未重載，面板明示統計服務待更新，不使用舊可用率替代。

本次檔案：`backend/provider_acquisition.py`、`backend/api_routes/observability.py`、`backend/static/provider_sla_helpers.js`、`backend/static/provider_sla_panel.js`、`backend/static/styles/provider_acquisition.css`、`backend/static/style.css`、`backend/static/index.html`、對應 acquisition tests、`tests/test_static_history_filters.py` 及 architecture map 的面板說明。

後續整合：確認同專案其他修改已完成並具驗證證據，再由正式 `start_mac.command` 保留既有 queue/Redis/報告狀態重載，查證 readiness、runtime identity 及正式 acquisition API。不可將本紀錄視為已完成正式部署。
