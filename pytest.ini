[pytest]
markers =
    live: tests that call external APIs or run long live workflows
    integration: broader integration tests that may use local services
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated:starlette.exceptions.StarletteDeprecationWarning
